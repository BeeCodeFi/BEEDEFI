import { PhasePlaceholder } from "@/components/ui/PhasePlaceholder";

export default function SettingsPage() {
  return (
    <PhasePlaceholder
      title="Settings"
      phase={6}
      description="Configure your agents, connect AI providers, manage memory and data sources. Coming alongside the AI integration in Phase 6."
    />
  );
}
